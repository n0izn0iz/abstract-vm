#include <string>
#include <iostream>

int	main(void) {
	std::map<std::string, Instruction*>
	std::string str;
	while (std::cin.good()) {
		std::getline(std::cin, str);
		std::cout << "Result: " << str << std::endl;
	}
	return (0);
}
